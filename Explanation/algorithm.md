 **ماشین بردار پشتیبان (Support Vector Machine یا SVM)** 
---
## 📌 فهرست مطالب

1. **الگوریتم SVM چیست؟**  
2. **ایده اصلی SVM: حداکثر کردن حاشیه (Margin Maximization)**  
3. **بردارهای پشتیبان (Support Vectors) چیستند؟**  
4. **الگوریتم SVM در حالت خطی (Linear SVM)**  
5. **وقتی داده‌ها خطی‌جداشدنی نیستند: هسته‌ها (Kernels)**  
6. **انواع هسته (Kernel Functions)**  
7. **پارامترهای مهم SVM: C و γ (گاما)**  
8. **مثال‌های ساده و تصویری**  
9. **مزایا و معایب SVM**  
10. **کاربردهای واقعی SVM**  
11. **پیاده‌سازی ساده در پایتون (اختیاری)**  

---

## 1. الگوریتم SVM چیست؟

**ماشین بردار پشتیبان (Support Vector Machine)** یک الگوریتم **یادگیری نظارت‌شده (Supervised Learning)** است که عمدتاً برای **طبقه‌بندی (Classification)** استفاده می‌شود، اما می‌توان از آن برای **رگرسیون** (با نام SVR) نیز استفاده کرد.

هدف اصلی SVM:
> **یافتن بهترین مرز تصمیم‌گیری (Decision Boundary)** بین کلاس‌ها به‌گونه‌ای که **حاشیه (Margin)** بین کلاس‌ها **حداکثر** شود.

---

## 2. ایده اصلی SVM: حداکثر کردن حاشیه

در بسیاری از الگوریتم‌های طبقه‌بندی (مثل Logistic Regression)، هدف این است که **تمام نقاط را درست طبقه‌بندی کنیم**.  
اما SVM یک **هدف هوشمندانه‌تر** دارد:

> **نه تنها مرزی پیدا کن که داده‌ها را جدا کند، بلکه مرزی پیدا کن که "اطمینان‌بخش‌ترین" جداکننده باشد.**

### 🎯 تعریف "حاشیه" (Margin):
- فاصله بین مرز تصمیم‌گیری و **نزدیک‌ترین نقاط** از هر کلاس.
- الگوریتم SVM سعی می‌کند این فاصله را **بیشینه** کند → **مرز با حاشیه بزرگ‌تر، تعمیم‌پذیری بهتری دارد**.

### 💡 چرا حاشیه مهم است؟
مرزی با حاشیه کوچک ممکن است به‌خوبی داده‌های آموزشی را جدا کند، اما در داده‌های جدید (تست) خطا داشته باشد (بیش‌برازش).  
اما مرزی با **حاشیه بزرگ** مقاوم‌تر است و **خطا را کاهش می‌دهد**.

<img width="1368" height="298" alt="image" src="https://github.com/user-attachments/assets/37fae803-8361-4432-942b-b6b9e17337ae" />



---

## 3. بردارهای پشتیبان (Support Vectors) چیستند؟

- **تنها نقاطی** که روی یا نزدیک‌ترین فاصله به مرز تصمیم هستند.
- این نقاط، **کل مرز تصمیم‌گیری** را تعیین می‌کنند.
- اگر بقیه نقاط را حذف کنیم، مرز تغییری نمی‌کند — فقط **بردارهای پشتیبان مهم هستند**!

> ✨ نام "ماشین بردار پشتیبان" از همین ویژگی گرفته شده است.

<img width="466" height="297" alt="image" src="https://github.com/user-attachments/assets/8b975824-5288-4415-88dd-30f3cdbf993f" />


---

## 4. الگوریتم SVM در حالت خطی (Linear SVM)

وقتی داده‌ها به‌صورت **خطی‌جداشدنی (Linearly Separable)** باشند، SVM یک **خط (در 2D)، صفحه (در 3D) یا ابرصفحه (در nD)** پیدا می‌کند که دو کلاس را از هم جدا می‌کند.

### مثال ساده (دو کلاس: 🔴 و 🔵)

فرض کنید داده‌های شما در صفحه 2 بعدی هستند:

- کلاس مثبت (🔵): نقاط بالایی  
- کلاس منفی (🔴): نقاط پایینی  

الگوریتم SVM یک **خط مستقیم** پیدا می‌کند که:
- همه 🔵 را در یک طرف و همه 🔴 را در طرف دیگر قرار دهد.
- **فاصله** این خط تا نزدیک‌ترین 🔵 و 🔴 **حداکثر** باشد.

این خط به‌صورت ریاضی با معادله زیر نمایش داده می‌شود:
$\[
\mathbf{w} \cdot \mathbf{x} + b = 0
\]$
که در آن:
- $\mathbf{w}$: بردار وزن (عمود بر مرز)
- $b$: سوگیری (bias)

و دو خط موازی که حاشیه را تعریف می‌کنند:
$\[
\mathbf{w} \cdot \mathbf{x} + b = +1 \quad \text{(برای کلاس +1)} \\
\mathbf{w} \cdot \mathbf{x} + b = -1 \quad \text{(برای کلاس -1)}
\]$

عرض حاشیه = $\(\frac{2}{\|\mathbf{w}\|}\)$  
پس برای **بزرگ‌تر کردن حاشیه**، باید $\|\mathbf{w}\|$ را **کوچک‌تر** کرد.


<img width="517" height="437" alt="image" src="https://github.com/user-attachments/assets/b021202d-de47-48db-87fc-79e3c2744684" />


---

## 5. وقتی داده‌ها خطی‌جداشدنی نیستند: هسته‌ها (Kernels)

در دنیای واقعی، داده‌ها معمولاً **خطی‌جداشدنی نیستند**.  
مثال: داده‌هایی که به‌صورت دایره‌ای یک کلاس در وسط و کلاس دیگر در اطراف است.

در اینجا، SVM از **تکنیک هسته (Kernel Trick)** استفاده می‌کند.

### 💡 ایده Kernel Trick:
- داده‌ها را به یک **فضای با ابعاد بالاتر** نگاشت کن، که در آن **خطی‌جداشدنی** باشند.
- اما **بدون نیاز به محاسبه صریح** مختصات در فضای جدید!

> این کار با استفاده از **توابع هسته (Kernel Functions)** انجام می‌شود.

---

## 6. انواع هسته (Kernel Functions)

### الف) **هسته خطی (Linear Kernel)**
- مناسب برای داده‌هایی که تقریباً خطی‌جداشدنی هستند.
- سریع‌ترین گزینه.
- معادل: $\( K(\mathbf{x}_i, \mathbf{x}_j) = \mathbf{x}_i \cdot \mathbf{x}_j \)$

### ب) **هسته چندجمله‌ای (Polynomial Kernel)**
- مرز تصمیم‌گیری منحنی‌شکل می‌شود.
- فرمول: $\( K(\mathbf{x}_i, \mathbf{x}_j) = (\mathbf{x}_i \cdot \mathbf{x}_j + c)^d \)$
- متغیر $d$: درجه چندجمله‌ای

### ج) **هسته گاوسی / RBF (Radial Basis Function)**
- **رایج‌ترین** هسته برای داده‌های غیرخطی.
- فرمول: $\( K(\mathbf{x}_i, \mathbf{x}_j) = \exp(-\gamma \|\mathbf{x}_i - \mathbf{x}_j\|^2) \)$
- متغیر $\gamma$: کنترل‌کننده "پیچیدگی" مرز (هرچه بیشتر، مرز پیچیده‌تر)

### د) **هسته سیگموید (Sigmoid)**
- شبیه لایه‌های شبکه عصبی.
- کمتر استفاده می‌شود.

---

## 7. پارامترهای مهم SVM

### الف) **پارامتر C (Regularization Parameter)**
- کنترل می‌کند که مدل چقدر به **داده‌های آموزشی وفادار باشد**.
  - **پارامتر C بزرگ**: مدل سعی می‌کند **تمام نقاط را درست طبقه‌بندی کند** → ممکن است **بیش‌برازش** شود.
  - **پارامتر C کوچک**: مدل **حاشیه را بزرگ‌تر می‌کند**، حتی اگر چند خطا داشته باشد → **تعمیم بهتر**.

> پارامتر C معادل معکوس λ در رگرسیون خطی است.

### ب) **پارامتر γ (گاما) در هسته RBF**
- کنترل می‌کند که هر نمونه چقدر بر دیگر نمونه‌ها تأثیر بگذارد.
  - **پارامتر γ بزرگ**: نزدیکی‌ها مهم‌ترند → مرز پیچیده، خطر **بیش‌برازش**.
  - **پارامتر γ کوچک**: تأثیر گسترده‌تر → مرز ساده‌تر، خطر **زیربرازش**.

---

## 8. مثال‌های قابل فهم

### ✅ مثال ۱: طبقه‌بندی ساده (خطی)

فرض کنید می‌خواهیم مشتریان را به دو گروه **"پرخطر"** و **"کم‌خطر"** بر اساس **درآمد** و **بدهی** تقسیم کنیم.

- داده‌ها تقریباً خطی‌جداشدنی هستند.
- الگوریتم SVM با **هسته خطی** یک خط رسم می‌کند که بهترین فاصله را دارد.
- نقاطی که روی لبه‌ها هستند = **بردارهای پشتیبان**.

### ✅ مثال ۲: داده‌های غیرخطی (با RBF)

فرض کنید نقاط آبی داخل یک دایره و نقاط قرمز بیرون آن هستند.

- در فضای 2D، هیچ خطی نمی‌تواند آن‌ها را جدا کند.
- SVM با **هسته RBF**، داده‌ها را به فضای 3D نگاشت می‌کند (مثلاً به سطح یک کلاه!)، که در آن یک **صفحه** آن‌ها را جدا می‌کند.
- اما ما این فضا را نمی‌بینیم — فقط نتیجه نهایی را در فضای اصلی می‌بینیم: یک **مرز منحنی دایره‌ای**!


<img width="800" height="300" alt="image" src="https://github.com/user-attachments/assets/43183d7e-3bac-4e7f-9b22-31cd597095d4" />


<img width="800" height="300" alt="image" src="https://github.com/user-attachments/assets/2b842ce7-798d-4863-a76f-e53400306231" />


---

## 9. مزایا و معایب SVM

### ✅ مزایا:
- عالی برای **داده‌های با ابعاد بالا** (مثلاً پردازش متن).
- مؤثر حتی وقتی **تعداد نمونه‌ها کمتر از تعداد ویژگی‌ها** باشد.
- در صورت انتخاب مناسب هسته و پارامترها، **دقت بالایی** دارد.
- استفاده از **حاشیه** باعث **تعمیم بهتر** می‌شود.
- فقط به **بردارهای پشتیبان** وابسته است → حافظه کارآمد.

### ❌ معایب:
- **کند** برای داده‌های بسیار بزرگ (مقیاس‌پذیری ضعیف: $O(n^2)$ تا $O(n^3)$).
- **نیاز به تنظیم دستی پارامترها (C, γ)**.
- **تفسیرپذیری پایین**: نمی‌توان به‌راحتی فهمید چرا یک پیش‌بینی انجام شده.
- **حساس به نویز و پرت‌ها** (چون بردارهای پشتیبان تحت تأثیر پرت‌ها قرار می‌گیرند).

---

## 10. کاربردهای واقعی SVM

- **تشخیص چهره** (Face Recognition)
- **طبقه‌بندی متن** (Spam Detection, Sentiment Analysis)
- **تشخیص دست‌نوشته** (Handwriting Recognition)
- **پیش‌بینی بیولوژیکی** (پیش‌بینی پروتئین‌ها)
- **پردازش تصویر** (Image Classification)
- **شناسایی ناهنجاری** (Anomaly Detection)

---

## 11. پیاده‌سازی ساده در پایتون (با scikit-learn)

```python
from sklearn import datasets
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.svm import SVC
from sklearn.metrics import accuracy_score

# بارگذاری داده (مثلاً Iris)
iris = datasets.load_iris()
X, y = iris.data, iris.target

# تقسیم داده
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=42)

# نرمال‌سازی (ضروری برای SVM!)
scaler = StandardScaler()
X_train = scaler.fit_transform(X_train)
X_test = scaler.transform(X_test)

# ساخت مدل SVM با هسته RBF
svm = SVC(kernel='rbf', C=1.0, gamma='scale')
svm.fit(X_train, y_train)

# پیش‌بینی و ارزیابی
y_pred = svm.predict(X_test)
print("دقت:", accuracy_score(y_test, y_pred))
```

> ⚠️ **نکته مهم**: همیشه قبل از استفاده از SVM، داده‌ها را **استانداردسازی** کنید، چون SVM بر پایه فاصله است و به مقیاس حساس است (مثل KNN).

---

## 🧠 جمع‌بندی کلیدی

| مفهوم | توضیح |
|-------|--------|
| **هدف SVM** | یافتن مرزی با **بیشترین حاشیه** بین کلاس‌ها |
| **بردارهای پشتیبان** | تنها نقاطی که مرز را تعیین می‌کنند |
| **هسته (Kernel)** | ابزاری برای **تبدیل غیرخطی** داده‌ها بدون محاسبه صریح ابعاد بالا |
| **C** | تعادل بین **حاشیه بزرگ** و **خطای کم** |
| **γ** | کنترل **پیچیدگی** مرز در هسته RBF |
| **نرمال‌سازی** | **ضروری** برای عملکرد صحیح SVM |

